export default function BinderIndexPage() {
  return (
    <div className="flex items-center justify-center h-full text-muted-foreground">
      <p>Select a binder from the sidebar or create a new one.</p>
    </div>
  );
}
